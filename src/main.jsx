import React from "react";
import ReactDOM from "react-dom/client";
import LuobiApp from "./LuobiApp.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <LuobiApp />
  </React.StrictMode>
);
